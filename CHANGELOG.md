# The Binding of dodo CHANGELOG

《The Binding of dodo》是一个致敬《以撒的结合》的肉鸽地牢网页游戏。这份文档记录每次提交的更新内容，版本号按语义化版本标注，最新在上。

知识库文档（内网访问，另附玩法说明与截图）：https://ku.baidu-int.com/knowledge/HFVrC7hq1Q/pKzJfZczuc/4i9XFeQzYr/AtyPKxr4zXpRmD


## v0.13.0 — 2026-09-23

轻松档从「比标准档低 15%」改成「真的能安心推前期」，并让第 1、2 层不再生成小 Boss。
标准 / 硬核两档这一轮一个数都没动——新加的旋钮只写在轻松档的预设里，`diffMul()` 落到
未定义的键时返回 1，所以另外两档根本看不到它们。

- **第 1 层不再出现小 Boss**：小 Boss 房是「把普通房顶掉成两层之前那个 Boss」，第 1 层
  本来就只有一场 Boss，再插一场半血的更没道理；第 2 层一并关掉（它拿到的是 Gluttono
  的半血版本，而玩家那时手上通常只有一两件道具，这一战比第 2 层的正牌 Boss 还突兀）。
  第 3 层起仍是 50%。门是加在 `depth >= 3` 上的，但 `chance(0.5)` 照常抽一次再丢弃结果
  ——少抽这一下会让同一颗种子里后面所有房间（含秘密房的选点）整体平移，所以随机流长度
  刻意保持不变，第 1 / 2 层现在也有一条「同种子三档一致」的断言盯着。
- **第一个 Boss 不再对所有人一样重**：原来 `makeBoss` 对第 1 层不做任何缩放，理由是
  「新玩家的第一场永远一样」。可这一场的实际代价是：刚下地牢、零道具的裸装 dodo 只有
  8.3 DPS，要在 Gluttono 面前站桩硬射 36 秒，而它只有 6 颗半心。教学战三档都不进血量
  窗口这条保留（弱 build 不会被抬、强 build 不会被压），但轻松档另给一个 `openingBoss`
  旋钮：300 × 0.65 = 195 血，裸装压到 23 秒。标准 / 硬核仍是 300。
- **轻松档九条旋钮集体下调**：敌人血量 0.85 → 0.6、刷怪数 0.80 → 0.7、速度 0.92 → 0.85、
  伤害 0.85 → 0.6（深处接触伤害从 3 半心降到 2）、Boss 血量窗口 0.85 → 0.8、出招间隔
  1.15 → 1.5 倍、弹速 0.92 → 0.82。
- **前两层额外宽容一层**：新增 `openingEase`（0.7，只作用于第 1-2 层），乘进敌人血量、
  速度、接触与弹幕伤害、弹速。第 1 层实机效果：gaper 血量 12 → 5.0、速度 80 → 48 px/s、
  每房小怪 2.9 → 1.9 只。伤害那一项写成 `Math.max(1, …)` 是必须的——0.6 × 0.7 = 0.42
  会被四舍五入成 0，小怪从此碰不到人，再宽容的一档也得留下半颗心。
- **轻松档挨打后有更久的喘息**：新增 `invulnMul`，受击无敌帧 1.1s → 1.32s（标准 / 硬核
  仍是 1.1s）。
- **轻松档前两层回血概率 ×3**：清房掉半心 6% → 18%（第 3 层起回到 6%，标准 / 硬核全程
  仍是 6%）。这一段是玩家还没有任何回复道具、也还没有任何 build 的阶段，把容错放在这里
  比全局加血更对症。
- **测试**：487 → 511 条。第 1 层教学 Boss 那条「三档都是 300」按新规则拆成四条（标准 /
  硬核仍 300、轻松 = 血表 × 开局旋钮、教学战三档都不进窗口、裸装 36s → 24s 以内）；
  轻松档旋钮改为锁具体值；新增「开局宽容只作用于第 1-2 层」（第 1、2 层比值 0.42、第 3 层
  回到 0.6）、「第 1 层接触伤害不会被四舍五入成 0」（锁住 `Math.max(1, …)`）、「前两层掉心
  18% / 第 3 层起 6% / 标准与硬核全程 6%」（走真实 `onRoomCleared` 统计 2000 次）、
  「受击无敌帧 1.32s vs 1.1s」（走真实 `hurtPlayer`）、「同种子在第 1 / 2 层三档一致且
  没有小 Boss 房」。小 Boss 房概率断言也从「第 6 层 30 次里出现 6-26 次」扩成按层统计。

一处与最初设想的差别：计划里第 1 层轻松档的算式写成「300 × 战斗内 1.7 × 0.65 × 0.85」，
但战斗内那个 1.7 并不存在（只有危险之路的 ×1.2，第 1 层走不到），而 0.85 的 Boss 血量旋钮
只作用于第 2 层起。真按那个式子算是 166 血，和同一份文档里「195 血」的表格自相矛盾。
最终按表格实现：**195 = 300 × 0.65**，第 1 层只吃开局这一个旋钮。掉心概率同理——原来写的
`0.06 × openingEase(0.7)` 算出来是 4.2%，比标准档还苛刻，与「6% → 18%」的结论相反，
所以拆成独立的 `openingHeal: 3`。

## v0.12.0 — 2026-09-22

一次纯呈现层的打磨：不动任何数值平衡，只补界面与反馈。逻辑问题靠断言抓，这一轮
改动大多「断言抓不到」，所以顺手把能观测到的部分写成了第 11 章套件
（`test/suites/11-ui-polish.cjs`，47 条）与触控手感断言（`test/touch-ui-layout.cjs` +8 条），
总数 440 → 487（触控另有 59 条）。

- **后期特效不再压住 HUD**：暗角与颗粒原来在最后一层合成，整个画面（注释里写着
  including HUD）都被盖住。实测血条区落在暗角 0.39 强度处、小地图约 0.26，再叠一层
  10% 的 overlay 颗粒噪声——玩家最常读的两块读数恰好是全屏被压得最暗、最毛的地方。
  现在顺序改成「场景 → 暗角/颗粒 → HUD / 面板 / 结算纸」，HUD 与图鉴文字不再被糊。
- **字号跟着画布缩放走**：游戏固定 960×576 再 CSS 缩放，手机上画布只有 ~650px
  （0.68×），11–16px 的 HUD 小字落到 7–11 CSS px——桌面能读、手上是一片灰。新增
  `UIK`（画布小于 960 时按 `960/实际宽` 放大，上限 1.3）与 `installFontScale()`：
  在 ctx 上挂一层 font 访问器，把 60 处字号统一乘上系数，`measureText` 用的是同一个
  缩放后的字体，所以靠测量排版的卡片不会错位。图鉴 11 条挤 6 行已经到顶，那一屏
  显式保持 1:1。
- **中文回退字体栈**：Georgia / Trebuchet MS 都没有汉字字形，所有中文界面文字此前
  落到了系统默认（macOS 苹方 / Windows 雅黑 / Android Roboto），同一套界面在三个
  平台是三种气质，楼层横幅与暂停面板的衬线味道完全丢失。两套栈现在都点名了中文伙伴
  （Songti SC / Noto Serif / PingFang SC / 微软雅黑）。
- **指针悬停与光标**：全项目此前只有 `pointerdown`，没有 `pointermove`、也没有任何
  `cursor:pointer`——六个可点区域（难度石牌、图鉴行、角色环、小地图、dumate 选项、
  暂停面板文档链接）只能靠点一下试试。新增 `hoverRegion()` 复用点击处理里已有的命中
  测试，主循环每帧重算（面板与抉择界面出现的那一刻就可悬停），并同步切手型光标。
  过程中修掉一个真 bug：`play` 分支在暂停时提前 return，把暂停面板的命中测试整个吞了。
- **屏幕级受击反馈**：受伤原来只在角色身上闪一下色、画面抖一下，玩家余光里没有任何
  提示，只剩一颗心时也没有持续警示。现在受伤点亮 0.45s 的边缘红晕，血量 ≤ 2 时屏幕
  边缘持续脉动（都是边缘泛光，不会挡住中间的弹幕）。
- **世界定格（hit-stop）**：此前全项目只有「被打中的那只敌人停 2 帧」，Boss 豁免，
  击杀与爆炸 0 帧——最大的打击和最小的打击手感一样。现在击杀定格 3 帧、Boss 死亡
  12 帧。实现上只把**世界**的时间步长置 0（敌人/弹幕/粒子），玩家的移动、开火、拾取、
  房间逻辑照常推进：一开始写成整个 `updatePlay` 早退，结果单帧调用变成空转，测试
  立刻抓到 7 条失败，也说明那样写等于吞掉玩家输入。
- **命中确认分层**：打中敌人 / 打墙 / 射程耗尽原来共用同一组 6 颗粒子和同一个音效，
  玩家无法确认子弹是否打到了东西。现在四种结局各自区分：命中敌人 9 颗粒子 + 3 滴血
  + 敌人 7% 膨胀回弹（hitPop）；打墙是灰色小碎屑 + 新加的闷音 `SFX.dull()`；射程耗尽
  只有一小团无声音的飞溅；骑士格挡保持原来的灰色。
- **Boss 血条加残影条**：白色滞后条追赶红条（`dt × 0.8`），一次大伤害能看出「掉了一截」，
  而不是血条悄悄变短。
- **转场补齐**：穿门原来是把玩家瞬移到对侧门口，中间什么都没有——`G.fadeT` 这个字段
  声明了却从未被使用。现在点亮 0.14s 的黑幕淡出。toast 原来只在最后 0.4s 淡出、出现是硬切，
  现在 0.16s 淡入 + 上浮 10px。暂停遮罩与死亡结算纸也都有入场（遮罩淡入 + 面板上浮 12px、
  纸从上方落入并回正倾斜）；暂停只淡遮罩、面板本身满不透明，否则头 9 帧面板是幽灵。
- **暂停面板变成能读的东西**：六维属性每项加一条相对**本角色基准值**的横条（中点是
  开局值）与 ±% 差异，绿/红着色——原来的裸数字回答不了「这算高还是低」。角色专属计量
  （怒气 / 魂火 / 复活）此前只在对局 HUD 里有，一暂停就消失，现在合并在状态行里。
  已拾取道具网格缩到 0.42× 时认不出是哪件，现在悬停会浮出名字与效果（复用房间里的卡片）。
- **Boss 狂暴、减速、被拒动作的反馈**：Boss 进入狂暴原来只是血条变色，现在带一声咆哮
  与震动；被减速的敌人脚下多一圈随剩余时间变淡的霜环（此前唯一的反馈是道具生效那一瞬）；
  买不起 / 没炸弹 / 充能不足这些「按不动」的动作原来完全静音，现在有 `SFX.deny()`。
- **环境音床**：音效全是 one-shot，房间在有事情发生之前是完全安静的，Boss 登场也是静音。
  新增一段两振荡器 drone（根音随章节下行，Boss 在场时打开滤波、暂停与结算时收掉），
  以及 Boss 登场 / 狂暴的 `SFX.roar()`、刷怪时的 `SFX.spawn()`。
- **爆炸有形状也有痕迹**：14 颗粒子之外补一圈扩散冲击环，地上留一块深色焦痕
  （复用 `drawStain` 的 `color` 字段）。
- **触控侧手感漏点**：摇杆加 14% 死区（原来拇指静止时的像素抖动会被读成移动，人会慢慢
  飘进弹幕）；射击键补 `touchcancel`（来电/通知打断触摸后方向会粘住、无限朝一个方向射击）；
  暂停/图鉴按钮在小屏只有 ~35px，用伪元素把命中区补到 ~43px（实测可点区域从 35 → 43，
  且两个按钮之间仍留 4px 死区不互相咬住）；炸弹/道具按钮跟着游戏状态
  灰化；`rot90` 假横屏下按钮组改吃物理顶部的安全区（原来刘海会压住按钮）。
- **无障碍**：canvas 里的 HUD 对读屏完全不可见，新增 `.sr-only` 的 `aria-live` 节点播报
  状态变化与 toast；触控 UI 里的真 `<button>` 补了 Enter/Space 激活路径与可见焦点环
  （此前只有 touchstart，外接键盘按不动）；受伤与 Boss 死亡带 `navigator.vibrate` 触感
  （Android 有、iOS 没有，有就震一下）。
- **顺手修掉的工具链 bug**：`test/readme-shots.cjs --gif` 把 `Date.now()` 墙钟时间戳写进了
  `times.json`，而该文件的约定是「每帧显示多久」（`gif-frames.cjs` 写的是固定 66.7ms）。
  结果是 `make-gifs.py` 算出几百万毫秒的 duration、Pillow 抛 `struct.error`，
  并且**会先把已存在的 GIF 覆盖成 800 字节的残file**。现在两条抓帧路径都写固定步长，
  GIF 按 README 记的流程重新生成。

### 本次提交涉及的文件

同一个提交里落下 v0.11.0 与 v0.12.0 两个版本：v0.11 动的是数值入口，v0.12 动的是绘制顺序、
时序与输入手感。括号里是行数（增/删），`js/utils.js`、`js/game-screens.js` 这类混合文件两边
都算，不重复计数。

**游戏逻辑**

- `js/utils.js`（+67/−0）—— 一个文件里装了两件事：难度预设 `DIFFICULTY_PRESETS` / `diffMul()`，
  以及排版工具 `UIK` / `uiF()` / `installFontScale()` 与中文回退字体栈。
- `js/game-core.js`（+230/−4）—— 排版与输入层：`installFontScale` 字号缩放、`hoverRegion` 指针悬停、
  摇杆 14% 死区、`syncTouchButtons` 按钮灰化、读屏播报与环境音床的调度。
- `js/boss-core.js`（+66/−22）—— Boss 血量改成「8s 下限 / 55s 上限 / 抬升封顶血表 ×1.5」双边窗口，
  `estimatePlayerDPS` 补齐光环泪与爆炸项，出招间隔与弹速乘难度旋钮。
- `js/entities-player.js`（+54/−13）—— 眼泪四种结局分开（命中敌人 / 打墙 / 格挡 / 射程耗尽），
  爆炸补扩散冲击环与地面焦痕。
- `js/items.js`（+49/−1）—— 新增 `ITEM_TIERS` 三档分层与随楼层走的权重表，抽选仍然只消耗一次随机。
- `js/entities-enemies.js`（+46/−14）—— 敌人血量 / 伤害 / 弹速乘难度倍率、速度随层缩放，
  击杀点亮世界定格，Boss 血条加残影条。
- `js/menu-unlocks.js`（+36/−9）—— 标题界面新增难度胶囊（点击循环 / ↑↓ 切换），
  图鉴面板绘制时把字号系数显式锁回 1。
- `js/game-flow.js`（+22/−5）—— 开局提示本局难度，穿门点亮 `G.fadeT` 黑幕，刷怪数乘难度、
  深度指数从 1.35 降到 1.15，Boss 房补一声咆哮。
- `js/game-update.js`（+21/−9）—— 世界定格：敌人 / 弹幕 / 粒子的更新压在受 `G.freeze` 驱动的步长上，
  玩家逻辑照常推进；买不起、没炸弹这类被拒动作补 `SFX.deny()`。
- `js/dungeon.js`（+14/−7）—— 地图字体纳入统一字体栈，小地图加悬停高亮描边。
- `js/meta.js`（+4/−1）—— 难度写进存档，读档后调用 `setDifficulty` 立即生效。

**绘制与音效**

- `js/game-screens.js`（+211/−58）—— 标题难度胶囊、角色环 / dumate 选项 / 文档链接的悬停高亮、
  暂停面板的属性相对条与难度行、道具格悬停浮出名字、结算纸落入动画。
- `js/game-render.js`（+104/−27）—— 暗角与颗粒提到 HUD 之前（HUD 不再被压暗），
  新增受击红晕与低血脉动、穿门黑幕、toast 淡入上浮、Boss 血条白色残影。
- `js/audio.js`（+61/−3）—— 新增 `dull` / `deny` / `spawn` / `roar` 四种音效，
  加一段随章节下行、Boss 在场时开滤波的环境音床。
- `style.css`（+47/−2）—— 触控控件按下过渡、`.corner-btn::after` 把命中区外扩、按钮灰化态、
  `:focus-visible` 焦点环、假横屏安全区、`.sr-only` 读屏样式。
- `js/sprites-fx.js`（+17/−0）—— 新增 `drawFrostRing`：被减速的敌人脚下画一圈随剩余时间淡出的霜环。
- `js/sprites-items.js`（+15/−11）—— 拾取物、商店价格与道具 tooltip 的字体纳入统一栈，
  tooltip 高度改为按字号系数重排。
- `index.html`（+2/−0）—— 新增 `aria-live` 的隐藏播报节点。
- `js/sprites-room.js`（+1/−1）—— 门上金币符号的字体从写死的 Trebuchet MS 换成统一常量。

**工具与测试**

- `test/suites/10-difficulty.cjs`（新增 411 行）—— 难度与数值曲线专项：三档预设、难度不污染种子、
  敌人数值缩放、刷怪数、Boss 双边窗口、道具三档分布、选择入口与存档、暂停与结算的难度行排版。
- `test/suites/11-ui-polish.cjs`（新增 394 行）—— 呈现层回归：字号缩放与中文回退、指针悬停、
  后期特效与 HUD 的绘制顺序、受击红闪 / 低血脉动 / 世界定格、命中四种结局、转场淡入、
  暂停面板、触控按钮与无障碍。
- `test/suites/02-bosses.cjs`（+91/−17）—— Boss 血量断言从「只封顶」翻成双边窗口，
  补弹幕字段齐备与「真的能打到玩家」的断言。
- `test/touch-ui-layout.cjs`（+73/−0）—— 新增触控手感小节：角落按钮命中区外扩、摇杆死区、
  按钮灰化、射击键 `touchcancel` 后方向不粘住。
- `test/suites/08-endgame.cjs`（+25/−7）—— 逐层记录 `hpWindow` 与难度旋钮，
  校验血量确实等于血表经 8s / 55s 窗口夹取。
- `test/readme-shots.cjs`（+8/−2）—— 抓帧时间戳改成固定 66.7ms，不再把墙钟时间写进 `times.json`。
- `test/make-gifs.py`（+4/−0）—— 补注释说明 `times.json` 的约定与墙钟时间戳踩过的坑。
- `test/suites/01-core.cjs`（+3/−0）—— 暂停遮罩断言改为等淡入结束再采样像素。
- `test/playtest.cjs`（+2/−0）—— `SUITES` 里挂上第 10、11 章，套件总数到 11 个。
- `screenshots/`（12 张场景图 + 2 段动图）—— 按新画法重制，README 与本文引用的就是这一批。

改完的验证状态：`node test/playtest.cjs` **487 / 487 通过**，`node test/touch-ui-layout.cjs`
**59 / 59 通过**，两条命令的退出码都是 0，全程控制台无报错。
分套件：01-core 46 / 02-bosses 55 / 03-items 69 / 04-rooms 67 / 05-pools-and-curses 37 /
06-map-dungeon 17 / 07-meta-branch 42 / 08-endgame 18 / 09-dumate 36 / 10-difficulty 53 /
11-ui-polish 47，合计 487（`helpers.cjs` 里的 `eq()`、`near()` 都转调 `ok()`，所以按行数数
`ok()` 会比运行时少，第 10 章 40 处写成 53 条、第 11 章 42 处写成 47 条）。

## v0.11.0 — 2026-09-22

难度从「一条单向橡皮筋」改成三层可调：Boss 血量变成双边窗口，后期压力从「血厚」搬到「更凶」，
道具按强度分三档抽，标题界面多了一块三档难度石牌。

- Boss 血量双边窗口：原来只按玩家站桩 60 秒输出封顶、不设下限，实际后果是中强 build 在第 3-6 层
  把 Boss 打成 5 秒。现在改为双边夹取——下限 = 站桩输出 × 8 秒，上限 = × 55 秒（原 60 秒略降），
  且下限最多把血量顶到血表的 ×1.5。弱 build 行为不变（仍被上限压住，第 6 层的 3800 只按 55 秒算），
  中配打血表原值，第 1 层教学血量 300 不受窗口影响。Boss 对象上记了 `hpWindow`，测试直接校验
  `clamp(血表, 下限, 上限)`。
- 抬升上限是这次的关键：只做「按玩家输出等比抬下限」的话，凡是够强到触到下限的 build 都会拿到
  同一个秒数，再强一倍也感觉不出来——那不是难度，是橡皮筋。加了 ×1.5 的抬升上限之后，够强
  只是「拿到 8 秒」，**超过上限的部分才真正换成速杀**（实测第 6 层：裸装 55s、中配 55s、满配
  7.9s → 数值堆满 2.4s；第 3 层满配 2.8s）。测试里按 5 档 build 强度锁了这条单调曲线。
- DPS 估算器补漏：加入环绕泪、光环泪（每 0.22 秒一跳）、爆炸（命中 + 落地爆炸 = 1.7 倍）与分裂
  倍率，激光蓄力改用真实的 `laserChargeTime(p)` 而不是常量 0.5，生气 dodo 按暴走上限（攻击
  ×2.5 / 间隔 ×0.5）折算窗口——不再随进房怒气浮动。在此之前一件 Godhead 就能让真实输出变成
  估算值的 2-4 倍，把 Boss 血量砍到四分之一；这个封顶机制原本在它最该起作用的地方是失效的。
- 后期压力搬家：血量第二段指数从 1.14 降到 1.10（第 12 层倍率 19.6× → 15.8×），省下的压力换成
  速度——敌人速度首次随楼层缩放（每层 +2%，第 12 层 +22%）；每房刷怪数的深度指数从 1.35 降到
  1.15，封顶从 10 放宽到 12（第 12 层 10 只 → 9 只，前中期不再一进深层就挤满）。
- 道具三档分层：92 件被动按强度分 30 朴素 / 43 中等 / 19 强力，抽选权重跟着楼层走（1-3 层
  6:3:1，4-8 层 3:5:2，9-12 层 1:4:5）。实测宝箱池里强力档占比从第 1 层 5.0% 涨到第 12 层 23.5%，
  朴素档从 47.5% 掉到 7.9%。抽选仍然只消耗一次 `rng()`，种子可复现性不变。
- 三档难度：标题界面右上角石牌，↑↓ 切换 / 点击循环，选择写进存档。九条旋钮（敌人血量、刷怪数、
  速度、接触与弹幕伤害、Boss 血量与窗口、出招间隔、弹速）只做乘法，不消耗任何随机流——同一个
  种子在三档下生成完全一样的地牢，测试里有一条断言专门盯着这件事。
- 顺手修掉的文档错账：README 里的道具数一直是 89（实际 92 件被动 + 6 件主动）；Boss 血量「每层
  约 1.32 倍」在代码里并不存在，那是一张手写常量表（F2→F12 几何平均 ×1.37）。
- 技能界面也显示本局难度（原来只有标题石牌 + 开局 toast）：暂停面板单独占一行，结算纸条在涂鸦
  下面补一行小字。暂停面板不留「接在楼层名后面」的方案是有意的——实测最长楼层名 + 最长诅咒名
  再加难度会到 386px，超过面板 380px 的内宽，拆开之后两行各自余量都很大，并加了断言锁住。
- 测试：新增 test/suites/10-difficulty.cjs（覆盖三档预设、种子一致性、敌人缩放、刷怪数、Boss
  窗口、bossHp 旋钮、怒气上限、分层分布、选择入口与持久化、暂停面板 / 纸条的难度行排版）；
  Boss 血量断言从「单向封顶 + 允许速杀」翻成双边窗口，通关流程改为逐层精确校验
  `clamp(血表, 下限, 上限)`。另新增一条「弹幕字段齐备 + 真的能打到玩家」的断言，属于补课：
  改造过程中 `bossShotFrom` 的字段被写坏过，`r` 变成 undefined 之后 Boss 弹幕既打不到人也不
  可见（画布对非有限半径直接跳过绘制），而当时的 416 条断言全绿——这类「数值字段缺失」的错
  只有专门的断言能抓。另有三条拦住 canvas `fillText`/`drawCrayonText` 的「确实画出了这行字」
  断言，覆盖暂停面板 / 死亡纸条 / 通关纸条上的难度显示。当前 440 / 440 通过。
  另：暂停面板的文字断言原本在按 P 后两帧采样，暂停遮罩改成 0.15s 淡入之后那段采样
  只拿到 ~20% 不透明度，已改为等淡入结束再采样（01-core.cjs）。

## v0.10.0 — 2026-08-25

数值规则与奖励保底三处大改：Boss 不再按玩家输出抬血量下限，迷失 dodo 的生命上限正式折成攻击力，秘密房与献祭房的高级道具改为必出。

- Boss 血量规则重做：原来第 2 层起按「玩家估算 DPS × 30 秒」保底抬血量（最终 Boss 保底 60 秒），叠满输出也至少要磨半分钟。现在改为只按玩家站桩 60 秒输出封顶、不设下限——Boss 保持设计血量，数值堆高的 build 允许 10 秒内速杀，低配也不会被拖进无穷磨血。最终 Boss 同规则；隐藏终极 Boss 仍按本局 MEGA dodo 实战血量 × 20 登场。
- 迷失 dodo 增强：生命上限整体折成攻击力。开局 3 颗心扣到半颗（两颗半星），折成 +15 攻击（原来只 +1）；之后拾到的加血道具同样不涨血条，每半颗心的上限增量折 +3 攻击（拾取提示注明「上限折攻击 +N」），「蘑菇之王」转变给的上限同样折算。牺牲上限的恶魔道具不受影响。
- 奖励保底：秘密房原来 35% 概率才摆一个随机道具座，现在必有高级道具；献祭房奖励表重做——前两次献祭掉金币，第 3 次必掉一件高级道具后祭坛沉寂，魂心不再作为献祭奖励。
- 测试同步：Boss 血量断言从 30s 下限翻成 60s 上限 + 允许速杀，迷失 dodo 新增初始攻击与加血道具转化断言。

## v0.9.1 — 2026-08-25

- 生气 dodo 怒气机制强化：
  - 打中敌人也会涨怒气（每命中 +2%，击杀 / 受伤照旧）；怒气只会在停手 2 秒后开始消退，越战越狂。
  - 怒气满进入暴走后大幅强化：攻击 ×2.5、攻速间隔 ×0.5、移速 +160（怒气条中段加成不变，仍是最多 +70% 攻击 / +32% 射速 / +50 移速）。
  - 处决新增特效与音效：血圈冲击波 + 血泉喷溅 + 「处决!」弹字 + 专属音效与屏幕震动。

## v0.9.0 — 2026-08-19

- 新增一位神秘隐藏终极 Boss。

## v0.8.0 — 2026-08-18

这轮把选人界面重做成以撒式的环形站位，解锁条件挪进按 P 打开的图鉴，两个老角色的机制推倒重做，再新增两个角色。测试从 304 项涨到 318 项，全部通过。

- 环形选人：5 个角色站成一圈，选中谁谁就转到最前面，其他人退到身后（旋转动画 + 近大远小）。键盘 ←/→ 转环，点击头像直达。未解锁的角色是暗色剪影。
- 解锁图鉴：标题界面按 P 打开（Esc 关闭），10 条解锁项每条带图标、说明和解锁条件，没解锁的画成灰黑剪影。原来堆在标题下方的解锁目标文字全部撤掉，界面清爽多了。
- 解锁弹窗：游戏中达成条件的瞬间弹出提示卡，道具当场发到手里、本局直接生效（角色解锁只提示不发东西）；之后的对局照常要在地牢里刷。多个解锁排队依次弹。
- 生气 dodo 重做：原来只是数值互换（高攻低血），玩起来和普通 dodo 没区别。现在是怒气槽角色——击杀 +9%、挨打 +40%，闲下来每秒退 5.5%；怒气越高攻击、射速、移速越强（最高 +70% / +32% / +50 移速），攒满进入暴走：眼泪染血、红色气场、接触伤害 +3。另有处决：血量被打到 15% 以下的小怪直接死，Boss 除外。HUD 炸弹旁边加了怒气条。
- 暗黑 dodo 重做：魂心体质保留，新增两条主动玩法。恶魔的宠儿：Boss 战后恶魔房必定开门（原来 33% / 66%），交易少收一颗心、魂心支付从 3 颗降到 2 颗，代价是 6 件圣物（dodo 光环 / 神圣之书 / 天使之翼 / 天使羽饰 / 圣痕斗篷 / 十字吊坠）永不进他的道具池。收割灵魂：击杀 12% 概率掉蓝色魂火，吸一团给主动道具充能一层，攒 3 团凝成半颗魂心，HUD 有计数。
- 迷失 dodo（新角色，累计死亡 25 次解锁）：只有半颗心，挨一下就死。换来飞行、幽灵眼泪、每房间一次圣盾，恶魔交易全部免费。生命上限和魂心对他完全无效——加多少都还是半颗心。
- 赌徒 dodo（新角色，单局同时持有 25 金币解锁）：开局 15 金币、幸运 +2、商店半价。每层重摇一次运势：随机两项属性上调、一项下调，换层先退掉上一层的再摇新的，不会越叠越多。运势跟着种子流走，同种子每层摇出来的都一样。
- 测试同步：解锁与角色套件按五角色更新，新增「角色专属机制」10 项断言（处决、魂火凝结、恶魔折扣、圣物禁池、迷失免死盾与免费交易、赌徒重摇与半价）。顺手修了个测试连锁问题：Boss 血量按玩家 DPS 校准，角色机制测试块跑完把选中角色留在赌徒上，后面的 12 层流程套件就以赌徒开局，每层运势重摇让 Boss 血量不再单调递增——现在测试块收尾会把角色还原成默认 dodo。当前 318 / 318 通过。

## v0.7.1 — 2026-08-18

这次只动结构，不动玩法，304 项断言跑完全过。之前 4 个源文件各自膨胀到一两千行，找段代码要翻半天，于是按职责拆成 15 个小文件，最大的不到 1000 行：

- `sprites.js`（2718 行）拆为 4 个：房间烘焙（`sprites-room`）、角色与敌人（`sprites-actors`）、拾取物与道具图标（`sprites-items`）、后期特效与 HUD（`sprites-fx`）。
- `bosses.js`（1852 行）拆为 4 个：Boss 定义与 AI（`boss-core`）、通用招式（`boss-attacks`）、专属招式（`boss-attacks-signature`）、Boss 绘制（`boss-render`）。
- `game.js`（1818 行）拆为 5 个：状态与输入（`game-core`）、局与楼层流程（`game-flow`）、每帧逻辑（`game-update`）、场景渲染（`game-render`）、界面与主循环（`game-screens`）。
- `entities.js`（1000 行）拆为 2 个：玩家与眼泪（`entities-player`）、敌人与弹幕（`entities-enemies`）。
- 测试同步拆分：2314 行的 `playtest.cjs` 拆为轻量入口 + `helpers.cjs` 公共设施 + `test/suites/` 下 8 个按主题编号的断言套件（章节间共享同一页面实例，按编号顺序执行）。
- 交付方式没变：还是普通 `<script>` 按顺序加载，没有 ES module、不用构建、没有 npm 依赖，双击 index.html 就能玩。

## v0.7.0 — 2026-08-18

这轮一口气补齐了以撒的核心随机性框架：道具分池、恶魔交易、种子系统、D6 重摇、献祭房、转变与诅咒楼层。局与局之间的差异和「本局要不要赌」的决策空间都大了一圈。

- 道具分池：89 件道具拆进三个池——宝箱池（默认）、商店池（12 件实用系：金币/幸运/吸附/地图等）、恶魔池（12 件代价系：恶魔之角/玻璃大炮/吐根糖浆/硫火之息等）。宝藏房抽宝箱池、商店首格上架商店池、恶魔交易只出恶魔池；池抽干自动回退全表，绝不空手。
- 恶魔交易房：击败 Boss 后有概率在 Boss 房旁裂开一扇黑门（无伤打完概率翻倍：33% → 66%），里面站着烛光中的恶魔雕像和两件恶魔池道具，标价 1 / 2 颗红心上限；红心上限不够时自动改收 3 颗魂心。付不起的话恶魔懒得理你。房间常暗（自带黑暗效果）。
- 魂心拾取物：世界里现在真的会掉蓝心了——清房 2% 概率、秘密房 30% 概率、献祭房与恶魔房固定产出。吃一颗 +1 魂心（上限 6 颗），受击先烧魂心再扣红心，HUD 蓝心排在红心之后（双层血条此前已就位，这次补上了获取渠道）。
- 种子系统：每局生成 8 位种子（暂停面板可见），标题界面按 S 可手动输入任意文字作种子。同种子 = 同楼层布局、同房型、同诅咒序列、同道具刷法（层生成走 种子×层数 派生流，进房结算道具也走房间种子流）。手动种子局与开发者模式同规则：不上排行榜、不计解锁。
- D6 六面骰：新主动道具（2 格充能），空格重摇本房间所有未拾取的道具，且按原池重摇：恶魔交易摇出来还是恶魔池（价格不变），宝藏摇出来还是宝箱池。配合分池，「这件不想要」终于有了答案。
- 献祭房（45% 概率生成）：房间中央一排献祭尖刺，踩上去扣一颗心并计数，奖励递进：金币 → 金币×2 → 魂心 → 炸弹/电池 → 宝箱池道具 → 魂心 → 恶魔池道具（第 7 次后祭坛沉寂、尖刺失效）。注意：飞行躲不过献祭尖刺——祭坛要的是真血。
- 二选一宝箱房：35% 的宝藏房改摆两件道具（危险之路的双倍宝藏除外），拿走一件另一件当场化为尘土。
- 转变系统：集齐 3 件同系道具触发质变（外观 + 能力，一局各触发一次）——
  - 羽翼圣者（羽毛系 7 件候选）：长出圣翼飞行 + 头顶光环 + 射速上升
  - 恶魔化身（恶魔系 10 件候选）：头生尖角 + 红瞳红雾 + 攻击 +2.2
  - 蘑菇之王（菌菇系 5 件候选）：戴上斑点菌盖 + 生命上限 +2 并回复 + 巨泪
- 诅咒楼层：第 2 层起每层 30% 概率随机一种诅咒（进层横幅紫字预告）——黑暗诅咒（全房陷入黑暗，只剩身边一圈光）、迷失诅咒（小地图与 Tab 全图全部失效）、未知诅咒（血条变成一个「?」，暂停面板同样遮蔽）。
- 测试同步：新增 8 个专项章节（分池与种子 / 魂心与双层血条 / 二选一 / 恶魔交易 / D6 / 献祭房 / 转变 / 诅咒楼层）共 37 项断言；顺手修掉地牢生成里一处绕过种子流的 Math.random。当前 304 / 304 通过。

## v0.6.0 — 2026-08-18

这轮把「一局之外的成长」补上了：解锁系统、两个新角色、以及第 4/8 层的分岔路线。参考以撒的长线设计——死亡也在积累进度，每一局都可能带来永久解锁。

- 解锁系统（进度存浏览器 localStorage，跨局永久生效）：
  - 6 件强力道具改为解锁后才进入道具池：无伤击败任意 Boss → 王者印记；无伤走完一整层 → 神之首；单局拾取 12 件道具 → 四重羽毛；通关一次 → 玻璃大炮；累计击杀 500 只 → 1UP!；累计死亡 10 次 → 死猫（死也有收获）。
  - 达成瞬间弹出「解锁」提示，死亡/通关结算纸上汇总本局新解锁，标题界面常驻展示接下来的解锁目标与进度。
  - 开发者模式的局不计解锁与击杀/死亡/通关累计（与排行榜同一条反作弊规则）。
- 新角色（标题界面 ←/→ 或点击头像选择，沿用 dodo 原形象小改，89 件道具与全部动画通用）：
  - 生气 dodo：低血高攻——2 颗心开局，攻击 +2.4、移速 +15，一对怒眉 + 泛红配色。累计击杀 300 只解锁。
  - 暗黑 dodo：魂心角色——2 颗红心 + 3 颗魂心开局，暗紫配色 + 灵气光环。魂心先于红心扣除（HUD 蓝心排在红心之后）；满血时吃到的红心/半心转为魂心储备（上限 6 颗）。通关一次解锁。
  - 未解锁的角色以黑色剪影站在选人界面上，脚下标注解锁条件；选中未解锁角色按开始会闪红提示。
- 分岔层：第 4 层与第 8 层击败 Boss 后出现两个地板门——
  - 左侧安稳门：照常进入下一层。
  - 右侧尖刺门（红刺环 + 红光脉冲）：进入「危险之路」——下一层敌人血量 ×1.35、每房刷怪 +1、Boss 血量 ×1.2；作为回报，宝藏房放两个道具座、Boss 击杀奖励翻倍。走近门口会浮出说明卡，进层横幅标红「危险之路」。
- 测试同步：新增「解锁系统与角色」（15 项）与「分岔层」（9 项）专项。当前 267 / 267 通过。

## v0.5.0 — 2026-08-18

这轮大幅强化了最终 Boss，恶魔房过路费改为真正的进出收费，并重调了全程难度曲线（此前普遍反馈前期偏难、后期偏简单）。

- MEGA dodo 全面强化：
  - 基础血量 26000 → 40000，且保底扛住 60 秒玩家估算 DPS（此前 30 秒），并且不受普通 Boss 的血量上限约束——终局战就该是一场马拉松。
  - 多技能同时释放：平时「主技能 + 1 个副技能」并行，狂暴（半血）后同时释放 3 个。副技能只从纯弹幕招式（风车 / 蝇卵炸弹 / 追踪光束）里选，与主技能互不重复；身体位移类招式只做主技能，避免精分。
  - 新增全屏技能「天启」：预警圈铺满整个房间、只在玩家附近留一个安全口（永远不在玩家脚下，站着不动必死），预警结束全场起爆并放射弹幕；狂暴时连放两轮且安全口换位。天启永远独占施放，保证安全口真实可用。
- 恶魔房（地图上的恶魔脸房间）过路费：进入和离开各扣半颗心，防御机制按直觉生效——飞行可飞过刺门，神圣护盾、减伤、无敌帧都能挡下；唯一的免费出路是用「回家的路」传送离开。
- 全程难度重平衡：
  - 前期减压：第 1-2 层刷怪数量下调一档；Boss 血量新增随层数放宽的上限（约 40+4×层数 秒 × 玩家估算 DPS），低配 build 不再在第 2 层就陷入 90 秒的磨血战。
  - 后期加压：第 7 层起小怪血量在原指数曲线上再乘一条 ×1.14^(层数-6) 的二次曲线，成型 build 不再一枪清房；敌方子弹伤害从 1-4 层的 0.5 心抬到 5-10 层的 1.5 心、11-12 层的 2 心。
  - 回血保持低概率：清房掉半颗心固定 6%，不做楼层差异化。
- 测试同步：新增最终 Boss 专项（血量下限、主副技能并行且互不重复、狂暴三连、天启铺满全场 / 留安全口 / 独占施放 / 全场起爆、副技能光束随 Boss 死亡清除）与恶魔房过路费专项（飞行 / 护盾 / 无敌帧 / 减伤各按直觉生效 + 传送免费），敌方子弹伤害曲线断言更新。当前 243 / 243 通过。

## v0.4.1 — 2026-08-17

- 清房奖励里回血（半颗心）的出现概率从 12% 降到了 6%，让血量更金贵一点。
- 商店调整：电池固定在最左货架、降价到 2 金币；红心涨价到 5 金币，挪到消耗品货架（与炸弹二选一上架，不再保证有货）。

## v0.4.0 — 2026-08-17

这轮加了全服排行榜，暂停界面也重排了一遍。

- 最速通关排行榜：通关即自动上榜，按用时排名（不含暂停时间），每人只保留自己的最佳成绩。数据存在线上动态数据服务里，本地双击 index.html 游玩时排行榜自动隐去，游戏本身不受影响。
- 暂停界面重排：左边属性面板，右边排行榜（前 8 名 + 自己的名次，就算掉出前 8 也能看到自己排第几）。
- 道具展示改为纯图标：以前效果名文字一长串会超出屏幕、图标超过 14 个还会被截断；现在全部道具只画图标，自动换行缩放，塞满 92 件也放得下。
- 暂停界面底部加了「更新日志与玩法说明」入口，点击直达在线文档，点其他区域照常继续游戏。
- 通关结算页会显示成绩上传结果（新纪录 / 未破纪录 / 上传失败）。
- 防作弊：开发者模式碰过的对局不上传成绩。

## v0.3.0 — 2026-08-17

这轮重做了全部 Boss 招式。以前 13 个 Boss 从同一个招式池里抽（散射呕吐 3 个 Boss 在用、全屏扫场激光 5 个 Boss 在用），打到后面全是似曾相识；现在 40 种攻击行为每招专属一个 Boss，没有任何两个 Boss 共用同一招。

- 招式表重排：散射呕吐收归第 1 层 Gluttono，全屏发散持续激光收归第 6 层 Mortuum，原有 15 招各归其主。
- 新增 25 个专属招式：
  - Duodeno（肠虫）：钻地潜行，土丘预警圈追着玩家跑，钻出瞬间放射爆弹
  - Larvato（巨蝇）：俯冲横掠，飞行路径两侧播撒幼虫弹隔出封锁带
  - Chubbler：吐出巨型慢速弹，飞行途中炸裂成放射弹幕
  - Osseo（骷髅）：骨骼迫击炮（多点红圈预警后爆开）+ 一对回旋骨镖
  - Utero（心脏）：带 63° 安全缺口、逐波旋转的收缩环 + 主动脉双螺旋喷血（随心跳脉动）
  - Sanguino（血怪）：血色光矛钉住玩家所在行列 + 血球空中分裂 + 惊惶乱射
  - Infernus（恶魔）：火焰吐息一波比一波宽而慢 + 从远端滚来的火墙（只留一个缺口）+ 近身余烬云
  - Seraphim（天使）：光环在玩家原地收束 + 天罚光柱（一根钉在玩家 x 坐标）+ 羽毛扇形飘散
  - Auricus（金像）：金币从四壁向中心挤压 + 宝石扇悬停半空再全体扑向玩家
  - Umbra（暗影）：半虚体幻影突进（穿过后回身扇形弹）+ 蛇形交织弹幕 + 召唤窥视之眼狙击
  - MEGA dodo：四臂风车弹幕（中途反转方向）+ 蝇卵炸弹（爆开兼孵化苍蝇）+ 持续追踪玩家的巨型光束 + 高空连环轰炸后砸地
- 基础设施：Boss 子弹支持从任意点发射（迫击炮 / 墙面 / 爆裂弹），地面预警圈支持多点同时显示并可指定半径，招式结束时统一恢复隐身状态。
- 测试同步：新增「招式全 Boss 专属互不重复」断言；「>= 5 个 Boss 掌握扫场激光」改为「收归单一 Boss 专属」；攻击行为种数下限从 10 提到 30。当前 229 / 229 通过。

## v0.2.1 — 2026-08-17

这轮全是测试。上一批玩法（炸弹、商店、挑战房）补上了自动化用例，另外专门写了一个复现「按住 Tab 焦点逃逸」的脚本。

- test/playtest.cjs 新增六个章节：商店房、炸弹、主动道具（空格）、新房型、小地图与全图、地图道具。覆盖首次进房摆货、炸弹爆炸与炸开隐藏墙、充能机制、各房型生成、地图道具的楼层揭示。
- 新增 test/tab-focus.cjs：用 CDP 可信键盘事件（rawKeyDown + autoRepeat）模拟按住 Tab 时的系统自动重复，复现焦点逃逸，并报告焦点最终落在哪个元素上，给后续修复定位。

## v0.2.0 — 2026-08-17

这轮加了炸弹、秘密房间、商店、挑战房，还有以撒标志性的空格主动道具。

- 炸弹系统：引信 1.6 秒，半径 95，伤害 26。炸敌人，也炸自己，站太近会白给。炸开隐藏墙能进秘密房间或暗道。
- 主动道具（空格键）：充能制，拾取时满充能，清房、电池、商店消费各充一格。首批五件：
  - 泪雨风暴（2 格）：重创房间内所有敌人
  - 妈妈的拥抱（3 格）：回两颗心
  - 回家的路（1 格）：传送回起始房间。战斗中逃走会重置该房敌人，防逃课
  - 炸弹锦囊（2 格）：立刻加两颗炸弹
  - 寒冬之息（4 格）：冻结所有敌人 5 秒
- 三件地图道具：指南针标出所有特殊房间，藏宝图显示整层布局，蓝图专门标秘密房间。
- 新房间类型：
  - 商店：首次进房摆货，老板只雇少量守卫
  - 诅咒房：60% 概率出现，中间放随机道具，40% 概率带宝箱
  - 挑战房：55% 概率出现，中间放着道具，拾取后关门开战，敌人分波从墙边传送围攻，全部清完才开门
  - 秘密房：藏在墙后，需炸弹炸开
  - 小 Boss 房：50% 概率顶掉一个普通房
  - 宝藏房有 25% 概率改成放主动道具
- 改动涉及 game、dungeon、items、entities、sprites 五个文件，新增炸弹、商店、挑战房等 Canvas 贴图。

## v0.1.1 — 2026-08-14

纯测试补充：playtest 覆盖 Brimstone 激光与多弹道并存时的协同表现，顺带补了 .gitignore。

## v0.1.0 — 2026-08-14

首发版本，游戏本体一次成型：

- 地牢：12 层随机生成，每层 5-10 个普通房加 Boss 房和宝藏房，八个视觉章节各有配色。小地图、双向门、岩石不堵门。
- 战斗：四向眼泪射击（每次射击 dodo 都会眨眼），十种敌人，受击无敌帧、命中切帧、具现化保护期。血量每层 ×1.22，第七层起弹幕翻倍还出追踪弹。
- Boss：十三个，含最终 Boss MEGA dodo。跳跃冲击、散射呕吐、激光预警、环形弹、召唤小怪、全屏扫场激光，行为库很全；半血狂暴；30 秒 DPS 兜底，输出叠满也秒不掉。
- 道具：89 件可叠加，Brimstone 激光、爆炸、分裂、弹墙、中毒、冰冻、幽灵弹、距离伤害、暴击、护盾、吸血、复活、飞行，机制一大把，属性有上下限保护。
- 飞行：dodo 之翼，八方向姿态，羽毛爆发加翅膀生长动画，无视地面障碍。
- 音效：WebAudio 实时合成，没有音频文件，爆炸是分层合成，听感接近真实。
- 界面：暂停面板看六维属性和生效效果，切窗口自动暂停。开发者模式（` 开关，[ ] 换道具）方便逐件试道具。死亡/通关有统计，进层有横幅。
- 移动端：虚拟摇杆加四向射击按钮。
- 测试：Playwright 无头验收，跑 node test/playtest.cjs。
- 零依赖：原生 ES2020 加普通 script 标签，素材全由 Canvas 绘制，双击 index.html 就能玩。
